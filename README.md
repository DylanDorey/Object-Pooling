# Object-Pooling
Object Pooling is used primarily for performance optimization and creates instances of all needed assets before gameplay to remove the action of instantiating large amounts of objects at runtime.
